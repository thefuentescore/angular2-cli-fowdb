import { AttributeSortPipe } from './attribute-sort.pipe';

describe('AttributeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
