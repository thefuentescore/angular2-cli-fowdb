import { FormatSortPipe } from './format-sort.pipe';

describe('FormatSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSortPipe();
    expect(pipe).toBeTruthy();
  });
});
